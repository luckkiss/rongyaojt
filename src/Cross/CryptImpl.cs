using System;

namespace Cross
{
	public class CryptImpl : ICrypt
	{
		public void Encrypt(uint seed, byte[] data, uint length)
		{
		}

		public void Decrypt(uint seed, byte[] data, uint length)
		{
		}
	}
}

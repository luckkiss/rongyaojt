using Cross;
using System;

namespace MuGame
{
	internal class _graphChaImgNumSprite : _graphChaSprite
	{
		public override void initShowInfo(Variant data, Action<_graphChaSprite> cb)
		{
			base.initShowInfo(data, cb);
		}

		public override void dispose()
		{
			base.dispose();
		}
	}
}
